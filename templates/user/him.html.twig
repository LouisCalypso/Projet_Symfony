{% extends 'base.html.twig' %}

{% block title %}{{ userVisited.username }}{% endblock %}

{% block body %}
    <!-- Page Header -->
    <header class="masthead p-5 my-3" >
        <div class="overlay"></div>
        <div class="container">
            <div class="site-heading text-center">
                <h1>{{ userVisited.username }}</h1>
            </div>
        </div>
    </header>

    <div class="container">
    {% for post in userVisited.posts %}
        {% include "post/post.html.twig" %}
    {% endfor %}
    </div>
{% endblock %}
